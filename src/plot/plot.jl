#=
This package has some functions help to plot images, sequences, trajectories, etc.
based on PyPlot.jl, 
so, PyPlot.jl must be installed before using these functions.
=#
include("plt/plt.jl")

# import KomaMRI.KomaMRIPlots: interp_map, theme_chooser, plot_koma
# import KomaMRI.KomaMRIPlots: plot_kspace, plot_seqd, plot_seq, plot_phantom_map
# include("plotly/plotly.jl")
